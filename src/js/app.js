import TasksTracker from './components/tasks-tracker/TasksTracker';

/* eslint-disable */
console.log('it works!');

const tasksTracker = new TasksTracker();
tasksTracker.bindToDOM(document.querySelector('.tasks-container'));


